{
  "name": "nexus-mcp-server",
  "version": "1.0.0",
  "description": "NEXUS — god-tier MCP server giving agentic coding agents deep code intelligence, change-impact awareness, persistent memory, engineered context, verified execution, and deterministic orchestration.",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=18" },
  "bin": { "nexus-mcp": "./dist/index.js" },
  "main": "./dist/index.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "node --watch dist/index.js",
    "check": "tsc -p tsconfig.json --noEmit",
    "test:client": "node test/client.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
