{
  "name": "nexus-mcp-server",
  "version": "1.0.0",
  "description": "NEXUS — god-tier MCP server giving agentic coding agents deep code intelligence, change-impact awareness, persistent memory, engineered context, verified execution, and deterministic orchestration. 31 tools, 20+ languages, zero native dependencies.",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=18" },
  "bin": { "nexus-mcp": "./dist/server.js" },
  "main": "./dist/server.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "node --watch dist/server.js",
    "check": "tsc -p tsconfig.json --noEmit",
    "test:client": "node test/client.mjs",
    "test:unit": "vitest run test/unit --reporter=verbose",
    "test:unit:watch": "vitest watch test/unit",
    "test": "npm run check && npm run build && npm run test:client && npm run test:unit",
    "benchmark": "vitest run bench --reporter=verbose",
    "ci": "npm run check && npm run build && npm run test:client"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^2.0.0"
  }
}
